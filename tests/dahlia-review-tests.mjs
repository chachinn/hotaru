import assert from 'node:assert/strict';
import { RELEASED_AVATAR_AUDIT_V45 as AVATARS } from './fixtures/released-avatar-audit-v45.mjs';
import { reviewedBuildProfile } from '../js/data/build-profiles/index.js';
import { DAHLIA_REVIEWED_TEAMS as TEAMS } from '../js/data/team-profiles/dahlia-reviewed.js';
import { compositionKey } from '../js/data/team-recommendations.js';
import { auditDahliaCompatibility, dahliaCompatibilityForCharacter, DAHLIA_COMPATIBILITY_POLICY } from '../js/data/character-compatibility/dahlia.js';
const profile=reviewedBuildProfile('Dahlia');
assert.ok(profile?.reviewed,'Dahlia must resolve to reviewed data');assert.equal(profile.variants.length,1);assert.equal(profile.variants[0].id,'shield-atk-speed-support');
const build=profile.variants[0].overrides;const samples=build.buildSummaryTeams||[];assert.ok(samples.length>=2&&samples.length<=3,'Dahlia Build Summary must stay at 2–3 representative teams');assert.deepEqual(build.mainStats.goblet,['HP%']);assert.ok(build.goalStats.some(row=>/240–270%/i.test(row.value)));assert.ok(build.goalStats.some(row=>/40,000/i.test(row.value)));assert.ok(build.goalStats.some(row=>/30% total/i.test(row.value)));assert.ok(build.goalStats.some(row=>/does not provide sustained teamwide healing/i.test(row.value)));assert.equal(profile.f2pWeapon,'Favonius Sword');
assert.equal(TEAMS.length,36);assert.equal(new Set(TEAMS.map(compositionKey)).size,TEAMS.length);assert.ok(TEAMS.every(team=>team.members.length===4&&team.members.includes('Dahlia')));assert.ok(TEAMS.every(team=>!/(game8|kqm|keqingmains|icy veins|hoyolab|reddit|youtube|fandom)/i.test(`${team.name} ${team.why} ${team.notes||''}`)));
for(const partner of ['Skirk','Escoffier','Furina','Wanderer','Faruzan','Bennett','Kamisato Ayato','Yoimiya','Arlecchino','Wriothesley','Clorinde','Sethos','Ningguang','Illuga','Freminet','Razor','Zibai','Aino'])assert.equal(dahliaCompatibilityForCharacter(partner).smartTeamApproved,true,`${partner} must be explicitly approved`);
const audit=auditDahliaCompatibility(AVATARS);assert.equal(audit.total,148);assert.ok(audit.rows.every(row=>row.status!=='invalid'));assert.ok(audit.rows.every(row=>row.status!=='unverified'||(!row.smartTeamApproved&&!row.adaptationAllowed)));assert.equal(dahliaCompatibilityForCharacter('Dahlia').status,'self');assert.equal(dahliaCompatibilityForCharacter('Lumine TPS').status,'not-applicable');assert.equal(dahliaCompatibilityForCharacter('Manekin Hydro').status,'not-applicable');
assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/Compatibility outranks build fit and account investment/i);assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/one reviewed ER\/HP Burst-support identity/i);assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/no sustained teamwide healing/i);assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/reliable off-field Hydro application/i);assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/Lunar-Crystallize/i);assert.match(DAHLIA_COMPATIBILITY_POLICY.rule,/Unverified pairings remain blocked/i);
console.log(`Dahlia review QA passed · ${profile.variants.length} build · ${TEAMS.length} distinct reviewed teams · ${audit.total}/148 compatibility records checked.`);