import layla from '../build-profiles/layla.js';import { LAYLA_REVIEWED_TEAMS } from '../team-profiles/layla-reviewed.js';const REVIEWED_AT='2026-08-24';const key=v=>String(v||'').trim().toLowerCase();const special=v=>/\bTPS\b|manekin/i.test(String(v||''));const aliases=new Map([['kazuha','Kaedehara Kazuha'],['ayaka','Kamisato Ayaka'],['kokomi','Sangonomiya Kokomi'],['raiden','Raiden Shogun'],['yae','Yae Miko']]);function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const a=aliases.get(key(raw));if(a)return a;for(const e of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${e}\\b`,'i').test(raw))return`${e} Traveler`;return raw}function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const s of [...(layla.sourceRefs||[]),...teams.map(t=>t.source)]){if(!s?.url||seen.has(s.url))continue;seen.add(s.url);out.push({...s,reviewedAt:s.reviewedAt||REVIEWED_AT})}return out}export function laylaCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(LAYLA_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='layla')return{character:raw,canonical:'Layla',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Layla cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=LAYLA_REVIEWED_TEAMS.filter(t=>t.members.some(m=>key(canonical(m))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(t=>t.id),builds:[...new Set(matching.map(t=>t.laylaBuild).filter(Boolean))],reactions:[...new Set(matching.map(t=>t.reaction).filter(Boolean))],reason:'Reviewed Layla evidence supports this pairing; exact shield-support or off-field-DPS identity, Cryo aura behavior, ER and 12-second coverage still depend on the full team.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Layla-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}export function auditLaylaCompatibility(names=[]){const rows=(names||[]).map(n=>laylaCompatibilityForCharacter(n)),counts={};for(const r of rows)counts[r.status]=(counts[r.status]||0)+1;return{character:'Layla',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(r=>r.smartTeamApproved).length,unverified:rows.filter(r=>r.status==='unverified').map(r=>r.character)}}export const LAYLA_COMPATIBILITY_POLICY={character:'Layla',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. HP shield support and off-field Cryo DPS are distinct Layla identities. Her shield and off-field Cryo application last 12 seconds, so 20-second rotations must not assume full coverage. C2 massively reduces Burst ER requirements but does not change shield duration. C6 substantially improves personal Skill/Burst damage but is not required for the off-field DPS identity. Layla is not a full-EM reaction trigger. Frozen Vapemelt shells must not use blunt Pyro carries such as Diluc, Klee or Yanfei because their blunt attacks cannot trigger the intended Vaporize or Melt against Frozen targets. Furina teams require practical sustain. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:layla.sourceRefs||[]};