import lisa from '../build-profiles/lisa.js';import { LISA_REVIEWED_TEAMS } from '../team-profiles/lisa-reviewed.js';const REVIEWED_AT='2026-08-24';const key=v=>String(v||'').trim().toLowerCase();const special=v=>/\bTPS\b|manekin/i.test(String(v||''));const aliases=new Map([['kazuha','Kaedehara Kazuha'],['kokomi','Sangonomiya Kokomi'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['ayaka','Kamisato Ayaka'],['dmc','Dendro Traveler']]);function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const a=aliases.get(key(raw));if(a)return a;for(const e of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${e}\\b`,'i').test(raw))return`${e} Traveler`;return raw}function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const s of [...(lisa.sourceRefs||[]),...teams.map(t=>t.source)]){if(!s?.url||seen.has(s.url))continue;seen.add(s.url);out.push({...s,reviewedAt:s.reviewedAt||REVIEWED_AT})}return out}export function lisaCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(LISA_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='lisa')return{character:raw,canonical:'Lisa',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Lisa cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=LISA_REVIEWED_TEAMS.filter(t=>t.members.some(m=>key(canonical(m))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(t=>t.id),builds:[...new Set(matching.map(t=>t.lisaBuild).filter(Boolean))],reactions:[...new Set(matching.map(t=>t.reaction).filter(Boolean))],reason:'Reviewed Lisa evidence supports this pairing; exact on-field Electro, off-field support or transformative-trigger identity, ER and reaction ownership still depend on the full team.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Lisa-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}export function auditLisaCompatibility(names=[]){const rows=(names||[]).map(n=>lisaCompatibilityForCharacter(n)),counts={};for(const r of rows)counts[r.status]=(counts[r.status]||0)+1;return{character:'Lisa',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(r=>r.smartTeamApproved).length,unverified:rows.filter(r=>r.status==='unverified').map(r=>r.character)}}export const LISA_COMPATIBILITY_POLICY={character:'Lisa',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. On-field Electro/Aggravate DPS, off-field Burst DPS/support, and transformative Reaction DPS are distinct Lisa identities. Standard Aggravate Lisa uses ATK or selective EM with Electro DMG and CRIT; full EM is incorrect there because substantial talent damage remains. Full EM is correct only when Lisa is the actual Hyperbloom or Overloaded trigger; reviewed Hyperbloom structures therefore exclude competing Electro core triggers and prioritize Level 90. Lisa Burst can target Dendro Cores. Her 15% DEF shred supports both reaction and non-reaction damage. Off-field Lisa has high ER requirements and dedicated support may skip Hold Skill to preserve field time. Furina teams require practical sustain. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:lisa.sourceRefs||[]};