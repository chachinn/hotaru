import iansan from '../build-profiles/iansan.js';
import { IANSAN_REVIEWED_TEAMS } from '../team-profiles/iansan-reviewed.js';
const REVIEWED_AT='2026-08-24';
const key=value=>String(value||'').trim().toLowerCase();
const special=value=>/\bTPS\b|manekin/i.test(String(value||''));
const aliases=new Map([['kazuha','Kaedehara Kazuha'],['ayato','Kamisato Ayato'],['kokomi','Sangonomiya Kokomi'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['heizou','Shikanoin Heizou'],['itto','Arataki Itto']]);
function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const alias=aliases.get(key(raw));if(alias)return alias;for(const element of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${element}\\b`,'i').test(raw))return`${element} Traveler`;return raw}
function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const source of [...(iansan.sourceRefs||[]),...teams.map(team=>team.source)]){if(!source?.url||seen.has(source.url))continue;seen.add(source.url);out.push({...source,reviewedAt:source.reviewedAt||REVIEWED_AT})}return out}
export function iansanTeamConstraintAllows(team={},options={}){const raw=options?.iansanConstellation,c=raw==null?null:Number(raw),min=team.constraints?.iansanMinConstellation;if(Number.isFinite(min)&&(!Number.isFinite(c)||c<min))return false;return true}
export function iansanCompatibilityForCharacter(value='',options={}){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(IANSAN_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='iansan')return{character:raw,canonical:'Iansan',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Iansan cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const all=IANSAN_REVIEWED_TEAMS.filter(team=>team.members.some(member=>key(canonical(member))===key(candidate))),matching=all.filter(team=>iansanTeamConstraintAllows(team,options));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(team=>team.id),reactions:[...new Set(matching.map(team=>team.reaction).filter(Boolean))],reason:'Reviewed Iansan evidence supports this pairing; full team selection must still preserve movement uptime, sustain, Scroll ownership, reaction restrictions and any constellation-gated low-movement structure.'};if(all.length)return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources:sourcesFor(all),teamIds:all.map(team=>team.id),reason:'Reviewed Iansan evidence exists, but the supplied constellation or movement assumptions do not authorize these structures.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Iansan-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}
export function auditIansanCompatibility(names=[],options={}){const rows=(names||[]).map(name=>iansanCompatibilityForCharacter(name,options)),counts={};for(const row of rows)counts[row.status]=(counts[row.status]||0)+1;return{character:'Iansan',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(row=>row.smartTeamApproved).length,unverified:rows.filter(row=>row.status==='unverified').map(row=>row.character)}}
export const IANSAN_COMPATIBILITY_POLICY={character:'Iansan',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. Iansan has one reviewed ATK-support identity: meet Burst ER first, then build ATK, with CRIT Rate only as needed for Favonius. The active carry must maintain enough movement to sustain Nightsoul Points and Iansan’s full Flat ATK buff; low-movement Clorinde structures that rely on Iansan C4 remain blocked without constellation evidence. 4pc Scroll is the default support set, but duplicate Scroll holders or Anemo/Geo targets that Electro cannot directly react with require an alternate support-set role rather than a fake second build. Iansan’s healing is modest and Furina teams require practical additional sustain. Chevreuse teams remain strictly Pyro/Electro-only. Iansan is not a full-EM transformative-reaction owner. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:iansan.sourceRefs||[]};