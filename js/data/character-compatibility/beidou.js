import beidou from '../build-profiles/beidou.js';
import { BEIDOU_REVIEWED_TEAMS } from '../team-profiles/beidou-reviewed.js';
const REVIEWED_AT='2026-08-24';
const key=value=>String(value||'').trim().toLowerCase();
const special=value=>/\bTPS\b|manekin/i.test(String(value||''));
const aliases=new Map([['kazuha','Kaedehara Kazuha'],['ayaka','Kamisato Ayaka'],['ayato','Kamisato Ayato'],['itto','Arataki Itto'],['kokomi','Sangonomiya Kokomi'],['childe','Tartaglia'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['mizuki','Yumemizuki Mizuki']]);
function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const alias=aliases.get(key(raw));if(alias)return alias;for(const element of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${element}\\b`,'i').test(raw))return`${element} Traveler`;return raw}
function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const source of [...(beidou.sourceRefs||[]),...teams.map(team=>team.source)]){if(!source?.url||seen.has(source.url))continue;seen.add(source.url);out.push({...source,reviewedAt:source.reviewedAt||REVIEWED_AT})}return out}
export function beidouCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(BEIDOU_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='beidou')return{character:raw,canonical:'Beidou',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Beidou cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=BEIDOU_REVIEWED_TEAMS.filter(team=>team.members.some(member=>key(canonical(member))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(team=>team.id),reactions:[...new Set(matching.map(team=>team.reaction).filter(Boolean))],reason:'Reviewed Beidou team evidence supports this pairing.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Beidou-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}
export function auditBeidouCompatibility(names=[]){const rows=(names||[]).map(beidouCompatibilityForCharacter),counts={};for(const row of rows)counts[row.status]=(counts[row.status]||0)+1;return{character:'Beidou',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(row=>row.smartTeamApproved).length,unverified:rows.filter(row=>row.status==='unverified').map(row=>row.character)}}
export const BEIDOU_COMPATIBILITY_POLICY={character:'Beidou',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. Smart Team may use only reviewed Beidou four-character structures. Preserve a driver who can actually trigger Stormbreaker, a practical battery/ER solution, Pyro/Electro-only restrictions in Chevreuse teams, a dedicated Hyperbloom trigger in Hyperbloom teams, and valid Stellar-Conduct enablement in Stellar teams. Raiden Burst attacks do not trigger Stormbreaker, so any Raiden pairing must preserve separate compatible field windows. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:beidou.sourceRefs||[]};