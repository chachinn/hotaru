import kirara from '../build-profiles/kirara.js';
import { KIRARA_REVIEWED_TEAMS } from '../team-profiles/kirara-reviewed.js';
const REVIEWED_AT='2026-08-24';const key=value=>String(value||'').trim().toLowerCase();const special=value=>/\bTPS\b|manekin/i.test(String(value||''));
const aliases=new Map([['kazuha','Kaedehara Kazuha'],['kokomi','Sangonomiya Kokomi'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['dmc','Dendro Traveler']]);
function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const alias=aliases.get(key(raw));if(alias)return alias;for(const element of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${element}\\b`,'i').test(raw))return`${element} Traveler`;return raw}
function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const source of [...(kirara.sourceRefs||[]),...teams.map(team=>team.source)]){if(!source?.url||seen.has(source.url))continue;seen.add(source.url);out.push({...source,reviewedAt:source.reviewedAt||REVIEWED_AT})}return out}
export function kiraraCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(KIRARA_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='kirara')return{character:raw,canonical:'Kirara',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Kirara cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=KIRARA_REVIEWED_TEAMS.filter(team=>team.members.some(member=>key(canonical(member))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(team=>team.id),builds:[...new Set(matching.map(team=>team.kiraraBuild).filter(Boolean))],reactions:[...new Set(matching.map(team=>team.reaction).filter(Boolean))],reason:'Reviewed Kirara evidence supports this pairing; exact shield-vs-driver role, Dendro uptime, trigger ownership, Nilou restriction and sustain depend on the complete team.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Kirara-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}
export function auditKiraraCompatibility(names=[]){const rows=(names||[]).map(kiraraCompatibilityForCharacter),counts={};for(const row of rows)counts[row.status]=(counts[row.status]||0)+1;return{character:'Kirara',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(row=>row.smartTeamApproved).length,unverified:rows.filter(row=>row.status==='unverified').map(row=>row.character)}}
export const KIRARA_COMPATIBILITY_POLICY={character:'Kirara',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. HP shield support and on-field Urgent Neko Parcel Dendro driver are distinct reviewed identities. Off-field Kirara has weak Dendro application and must not be treated as a high-uptime Dendro aura source; seed-heavy support teams normally need another reliable Dendro applier or a Dendro carry. HP is the primary shield-support stat. Full EM is only justified when on-field Kirara actually owns transformative reactions; Hyperbloom full EM belongs on Kuki Shinobu or off-field Raiden and Burgeon full EM belongs on Thoma. Nilou teams must remain Hydro/Dendro-only. Deepwood should only be assigned when Kirara can supply useful uptime and no more reliable holder exists. C6 teamwide Elemental DMG Bonus is a constellation benefit, not a separate build. Furina teams require actual healing beyond Kirara shielding. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:kirara.sourceRefs||[]};