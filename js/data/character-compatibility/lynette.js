import lynette from '../build-profiles/lynette.js';import { LYNETTE_REVIEWED_TEAMS } from '../team-profiles/lynette-reviewed.js';const REVIEWED_AT='2026-08-25';const key=v=>String(v||'').trim().toLowerCase();const special=v=>/\bTPS\b|manekin/i.test(String(v||''));const aliases=new Map([['kazuha','Kaedehara Kazuha'],['kokomi','Sangonomiya Kokomi'],['ayaka','Kamisato Ayaka'],['raiden','Raiden Shogun'],['dmc','Dendro Traveler']]);function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const a=aliases.get(key(raw));if(a)return a;for(const e of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${e}\\b`,'i').test(raw))return`${e} Traveler`;return raw}function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const s of [...(lynette.sourceRefs||[]),...teams.map(t=>t.source)]){if(!s?.url||seen.has(s.url))continue;seen.add(s.url);out.push({...s,reviewedAt:s.reviewedAt||REVIEWED_AT})}return out}export function lynetteCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(LYNETTE_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='lynette')return{character:raw,canonical:'Lynette',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Lynette cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=LYNETTE_REVIEWED_TEAMS.filter(t=>t.members.some(m=>key(canonical(m))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(t=>t.id),builds:[...new Set(matching.map(t=>t.lynetteBuild).filter(Boolean))],reactions:[...new Set(matching.map(t=>t.reaction).filter(Boolean))],reason:'Reviewed Lynette evidence supports this pairing; exact support, C6 driver, transformative ownership, sustain and constellation requirements still depend on the full team.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Lynette-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}export function auditLynetteCompatibility(names=[]){const rows=(names||[]).map(n=>lynetteCompatibilityForCharacter(n)),counts={};for(const r of rows)counts[r.status]=(counts[r.status]||0)+1;return{character:'Lynette',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(r=>r.smartTeamApproved).length,unverified:rows.filter(r=>r.status==='unverified').map(r=>r.character)}}export const LYNETTE_COMPATIBILITY_POLICY={character:'Lynette',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. Off-field VV support is Lynette’s default identity; C6 Anemo driver and C6 transformative trigger are separate identities and must remain constellation-gated. Pre-C6 Lynette does not apply enough Anemo to own Hyperbloom reliably. Full EM is only valid when C6 Lynette actually owns transformative reactions through Swirl; direct Electro core triggers such as Kuki Shinobu invalidate that ownership. Burst taunt is utility, not practical sustain. Furina teams require a real Fanfare/sustain plan. Bennett C6 can overwrite Lynette’s C6 Anemo infusion and must be treated as a caveat for driver teams. One-Skill rotations can have impractical ER needs; C4 or Sacrificial Sword can materially ease them. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:lynette.sourceRefs||[]};