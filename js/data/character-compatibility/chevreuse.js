import chevreuse from '../build-profiles/chevreuse.js';
import { CHEVREUSE_REVIEWED_TEAMS } from '../team-profiles/chevreuse-reviewed.js';
const REVIEWED_AT='2026-08-24';
const key=value=>String(value||'').trim().toLowerCase();
const special=value=>/\bTPS\b|manekin/i.test(String(value||''));
const aliases=new Map([['kazuha','Kaedehara Kazuha'],['ayaka','Kamisato Ayaka'],['ayato','Kamisato Ayato'],['itto','Arataki Itto'],['kokomi','Sangonomiya Kokomi'],['childe','Tartaglia'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['mizuki','Yumemizuki Mizuki']]);
function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const alias=aliases.get(key(raw));if(alias)return alias;for(const element of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${element}\\b`,'i').test(raw))return`${element} Traveler`;return raw}
function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const source of [...(chevreuse.sourceRefs||[]),...teams.map(team=>team.source)]){if(!source?.url||seen.has(source.url))continue;seen.add(source.url);out.push({...source,reviewedAt:source.reviewedAt||REVIEWED_AT})}return out}
export function chevreuseCompatibilityForCharacter(value='',chevreuseConstellation=0){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(CHEVREUSE_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='chevreuse')return{character:raw,canonical:'Chevreuse',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Chevreuse cannot pair with herself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=CHEVREUSE_REVIEWED_TEAMS.filter(team=>team.members.some(member=>key(canonical(member))===key(candidate)));if(matching.length){const nonC6=matching.filter(team=>!team.c6Only);const approved=nonC6.length>0||Number(chevreuseConstellation)>=6;return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:approved,adaptationAllowed:approved,sources:sourcesFor(matching),teamIds:matching.map(team=>team.id),reactions:[...new Set(matching.map(team=>team.reaction).filter(Boolean))],minimumChevreuseConstellation:nonC6.length?0:6,reason:approved?'Reviewed Chevreuse team evidence supports this pairing.':'Reviewed evidence supports this pairing only for C6 Chevreuse; lower constellations remain blocked.'}}return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Chevreuse-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}
export function auditChevreuseCompatibility(names=[],chevreuseConstellation=0){const rows=(names||[]).map(name=>chevreuseCompatibilityForCharacter(name,chevreuseConstellation)),counts={};for(const row of rows)counts[row.status]=(counts[row.status]||0)+1;return{character:'Chevreuse',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(row=>row.smartTeamApproved).length,unverified:rows.filter(row=>row.status==='unverified').map(row=>row.character)}}
export const CHEVREUSE_COMPATIBILITY_POLICY={character:'Chevreuse',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. Pure Overloaded Chevreuse requires every teammate to be Pyro or Electro so her A1 Pyro/Electro RES Shred remains active. Trigger Overloaded before the enhanced Hold Skill for her A4 ATK buff. Off-archetype Pyro or Electro support is C6-only and must never be assigned to C0–C5 accounts. Outside pure Pyro/Electro teams, do not claim A1 RES Shred. Quickswap DPS must not replace the HP support build merely because Chevreuse is more invested. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:chevreuse.sourceRefs||[]};