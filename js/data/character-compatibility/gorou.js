import gorou from '../build-profiles/gorou.js';
import { GOROU_REVIEWED_TEAMS } from '../team-profiles/gorou-reviewed.js';
const REVIEWED_AT='2026-08-24';
const key=value=>String(value||'').trim().toLowerCase();
const special=value=>/\bTPS\b|manekin/i.test(String(value||''));
const aliases=new Map([['itto','Arataki Itto'],['kazuha','Kaedehara Kazuha'],['kokomi','Sangonomiya Kokomi'],['raiden','Raiden Shogun'],['yae','Yae Miko'],['ayato','Kamisato Ayato']]);
function canonical(value=''){const raw=String(value||'').trim();if(!raw)return'';const alias=aliases.get(key(raw));if(alias)return alias;for(const element of ['Anemo','Geo','Electro','Dendro','Hydro','Pyro','Cryo'])if(new RegExp(`\\b(?:Aether|Lumine)\\s+${element}\\b`,'i').test(raw))return`${element} Traveler`;return raw}
function sourcesFor(teams=[]){const seen=new Set(),out=[];for(const source of [...(gorou.sourceRefs||[]),...teams.map(team=>team.source)]){if(!source?.url||seen.has(source.url))continue;seen.add(source.url);out.push({...source,reviewedAt:source.reviewedAt||REVIEWED_AT})}return out}
export function gorouCompatibilityForCharacter(value=''){const raw=String(value||'').trim(),candidate=canonical(raw),sources=sourcesFor(GOROU_REVIEWED_TEAMS);if(!raw)return{character:raw,canonical:candidate,status:'invalid',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Missing character name.'};if(key(candidate)==='gorou')return{character:raw,canonical:'Gorou',status:'self',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'Gorou cannot pair with himself.'};if(special(raw))return{character:raw,canonical:candidate,status:'not-applicable',smartTeamApproved:false,adaptationAllowed:false,sources:[],reason:'Special/TPS avatar records are not Smart Team characters.'};const matching=GOROU_REVIEWED_TEAMS.filter(team=>team.members.some(member=>key(canonical(member))===key(candidate)));if(matching.length)return{character:raw,canonical:candidate,status:'source-backed-compatible',smartTeamApproved:true,adaptationAllowed:true,sources:sourcesFor(matching),teamIds:matching.map(team=>team.id),reactions:[...new Set(matching.map(team=>team.reaction).filter(Boolean))],builds:[...new Set(matching.map(team=>team.gorouBuild).filter(Boolean))],reason:'Reviewed Gorou team evidence supports this pairing; Smart Team must preserve the carry family, Geo-count requirement, sustain assumptions, and either general ER/Favonius or Zibai short-rotation DEF-stacking identity.'};return{character:raw,canonical:candidate,status:'unverified',smartTeamApproved:false,adaptationAllowed:false,sources,reason:'No Gorou-specific reviewed team evidence authorizes Smart Team adaptation for this pairing.'}}
export function auditGorouCompatibility(names=[]){const rows=(names||[]).map(name=>gorouCompatibilityForCharacter(name)),counts={};for(const row of rows)counts[row.status]=(counts[row.status]||0)+1;return{character:'Gorou',reviewedAt:REVIEWED_AT,rows,total:rows.length,counts,smartTeamApproved:rows.filter(row=>row.smartTeamApproved).length,unverified:rows.filter(row=>row.status==='unverified').map(row=>row.character)}}
export const GOROU_COMPATIBILITY_POLICY={character:'Gorou',reviewedAt:REVIEWED_AT,rule:'Compatibility outranks build fit and account investment. Gorou is a dedicated DEF/Geo support and reviewed Smart Team structures should center on DEF-scaling Geo carries such as Zibai, Arataki Itto, or Noelle rather than treating him as a generic Geo slot. General Geo support prioritizes Burst ER and Favonius CRIT Rate; personal damage is low priority. Zibai short-rotation support is a distinct identity that often Bursts every other rotation, favors 4pc Instructor, and can trade ER for DEF to increase Ascendant Gleam’s Team Bonus. Prefer at least two other Geo teammates when maximizing Gorou’s Skill tiers and C6. C4 healing is minor and must not be treated as universal teamwide sustain; Furina structures require a practical healer such as Noelle or another explicitly supported sustain plan. Zibai Lunar-Crystallize teams must preserve a valid Hydro enabler and Ascendant Gleam structure. Unverified pairings remain blocked; TPS and Manekin records are not applicable.',sources:gorou.sourceRefs||[]};