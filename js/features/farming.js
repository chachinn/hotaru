function add(map,name,count){if(!name||!Number.isFinite(Number(count)))return;map.set(name,(map.get(name)||0)+Number(count))}
export function ascensionTotals(detail){const out=new Map(),mats=detail?.materials||{};if(Array.isArray(mats.ascensions))mats.ascensions.forEach(stage=>{(stage.mats||stage.materials||[]).forEach(item=>add(out,item.name,item.count||item.value));add(out,'Mora',Number(stage.cost||stage.mora_cost||0))});Object.entries(mats).forEach(([key,items])=>{if(!/^level_/i.test(key)||!Array.isArray(items))return;items.forEach(item=>add(out,item.name,item.value||item.count))});return[...out.entries()].map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count)}
export function talentTotals(detail,targetLevel=9,talents=3){const out=new Map(),mats=detail?.materials||{};if(Array.isArray(mats.talents)){const maxIndex=Math.max(0,Math.min(Number(targetLevel)-1,9));mats.talents.slice(0,Math.max(1,Number(talents))).forEach(track=>{if(!Array.isArray(track))return;track.slice(0,maxIndex).forEach(stage=>{(stage.mats||stage.materials||[]).forEach(item=>add(out,item.name,item.count||item.value));add(out,'Mora',Number(stage.cost||stage.mora_cost||0))})})}return[...out.entries()].map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count)}
export function mergeNeeds(...lists){const out=new Map();lists.flat().forEach(item=>add(out,item.name,item.count));return[...out.entries()].map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count)}
