name: Apply Build Intelligence 2 final

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  staged:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Build Intelligence 2 payload staging is intentionally paused."
